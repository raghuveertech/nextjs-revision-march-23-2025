import ContactComponent from './Contact';

export const metadata = {
  title: 'Contact Page',
};

const ContactPage = () => {
  return (
    <div>
      <ContactComponent />
    </div>
  );
};

export default ContactPage;
